/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.scelteperte.it', 'static.fanpage.it', 'www.giardinaggio.it'],
  },
}

module.exports = nextConfig
